export async function runInteractiveMode(): Promise<void> {
  process.stdout.write(
    'FilePilot interactive mode is not available yet.\n' +
      'Run "filepilot --help" to see available options.\n',
  )
}
